package com.gilbut.llmService.Handler.SttMessageHandlerTests;

public class SttMessageHandlerIntegrationTest {
    // 이건 외부 툴(https://hoppscotch.io/)을 사용하는 걸로
    // 이 테스트 할 때에는 application.yaml 의 ros.socket.uri 에 엔드포인트(/ros) 추가하기
    // 노션의 Use Case 참조
}
